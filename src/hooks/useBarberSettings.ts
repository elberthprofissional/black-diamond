// Re-export from context to avoid duplicate implementations.
// All components should use the single BarberSettingsContext source of truth.
export { useBarberSettings } from '../contexts/BarberSettingsContext';
